import { SelectedPage } from '@/shared/types'
import React from 'react'

type Props = {
    setSelectedPage:(value: SelectedPage) => void
}

const RoadSigns = ({setSelectedPage}: Props) => {
  return (
    <section 
    id="roadsigns"
    >
        
    </section>
  )
}

export default RoadSigns